# SpringAndAutumnGIS 开发问题记录

> 最后更新：2026-08-02
>
> 本文记录重构过程中已经出现的问题、根因、修复方式和以后必须遵守的约束。它不是临时聊天记录，而是后续地形、矢量编辑、离线瓦片开发时的回归检查表。

## 1. 经纬度方向与瓦片方位错误

### 现象

卫星影像能够显示，但大陆位置不正确，表现为南北或东西颠倒、瓦片之间地理内容无法连续。

### 根因

- 引擎世界轴、经纬度正方向和 XYZ 瓦片 Y 轴没有建立唯一约定。
- XYZ 使用左上角为原点，`y` 向南增加；地理纬度则向北增加。
- Web Mercator 的瓦片行不是纬度线性插值，不能按普通经纬度矩形直接映射。

### 当前处理

- 引擎世界坐标固定为：`Y` 指向北极，零经度位于 `+Z`，东经 90° 位于 `+X`。
- CPU 权威转换集中在 `CoordinateTransform` 和 `Ellipsoid`。
- XYZ 矩形由 `WebMercatorTilingScheme` 通过反 Mercator 公式计算。
- 栅格顶点在 GPU 中根据经度和 Mercator 参数投影到 WGS84 椭球。

### 长期规则

任何数据源都必须明确声明 CRS、瓦片方案和 Y 轴方向。禁止在渲染器内部通过交换坐标、翻转纹理等“试出来”的补丁修正方位。

## 2. ShaderMaterial 编译失败

### 现象

控制台出现：

- `isPerspectiveMatrix: no matching overloaded function found`
- `color: redefinition`
- `Vertex shader is not compiled`
- `useProgram: program not valid`

### 根因

- 手工拼接 Three.js 日志深度代码时，只加入了调用片段，没有完整遵循 Three.js 对应版本的 shader chunk 依赖。
- `ShaderMaterial` 开启 `vertexColors` 后，Three.js 已经注入 `color` 属性，再次声明造成重定义。
- 自定义 shader 与 Three.js 内置宏、属性和版本绑定过紧。

### 当前处理

- 使用 `#include <logdepthbuf_pars_vertex>`、`#include <logdepthbuf_vertex>`、`#include <logdepthbuf_pars_fragment>` 和 `#include <logdepthbuf_fragment>`。
- 不重复声明 Three.js 已经提供的 `color`、矩阵和公共函数。
- 坐标投影公共 GLSL 放在 `render/shaders/coordinates.ts`，避免多处复制后逐渐分叉。

### 长期规则

升级 Three.js 后必须实际打开页面检查 shader 编译日志；仅通过 TypeScript 和打包不能证明 WebGL shader 可用。

## 3. 地球过黑，瓦片效果不可辨认

### 现象

页面只有暗蓝色或接近黑色球体，卫星影像即使已加载也很难辨认。

### 根因

- 底球颜色、光照系数和纹理颜色空间叠加后整体亮度过低。
- 把展示用底球、遮挡球和真实地图表面混成了同一职责。

### 当前处理

- 纹理使用 sRGB 色彩空间。
- 栅格材质采用轻量环境增亮，不依赖场景灯光才能看清底图。
- 底球只承担无瓦片区域的回退和遮挡，不再代表最终地图表面。

### 长期规则

影像颜色、地形光照、矢量样式和大气效果必须位于不同渲染阶段，不能靠统一修改曝光掩盖某一层的问题。

## 4. 背面瓦片未剔除，可见数量接近预期两倍

### 现象

状态面板中的可见瓦片数量明显偏大，怀疑地球背面仍在参与选择和加载。

### 根因

只做普通视锥判断无法剔除椭球背面的瓦片；关闭 mesh 自带视锥裁剪后，如果四叉树选择器没有精确承担裁剪职责，网络和 GPU 都会处理无效瓦片。

### 当前处理

- `GlobeLodSelector` 在四叉树遍历阶段执行椭球地平线裁剪和相机视锥裁剪。
- 瓦片 mesh 的 `frustumCulled=false` 是有意设计：裁剪由 LOD 选择器统一负责，避免 Three.js 用不准确的局部包围体二次误裁。
- 状态面板分别显示遍历、地平线裁剪和视锥裁剪数量。

### 长期规则

网络请求必须发生在空间裁剪之后。地形、影像和矢量可以共享空间可见性结果，但各图层仍需按自身数据可用层级选择实际资源。

## 5. 视口边缘瓦片缺失，不同窗口表现不同

### 现象

- 局部进入视口的瓦片没有显示。
- 大于 20 级或低于 2 级时更容易看到未覆盖区域。
- 浏览器窗口尺寸或宽高比变化后，缺口位置和数量变化。

### 根因

- 早期用少量投影采样点判断瓦片是否进入视口。如果弯曲瓦片只有边缘穿过屏幕，而采样点都在屏幕外，就会被错误剔除。
- 屏幕空间误差只考虑固定尺寸，没有持续使用实际 viewport 高度和相机投影矩阵。
- 父节点的保守包围体可能与视锥相交，但四个子节点全部不可见；保留这种父节点会形成巨大的错误覆盖块。

### 当前处理

- 每帧按容器真实宽高更新相机 aspect、投影矩阵和 renderer 尺寸。
- 使用覆盖曲面采样点的保守世界空间包围球与完整视锥相交测试。
- 父节点拆分后如果所有子节点都被精确裁掉，则删除父节点，不把父节点作为虚假可见叶子保留。
- 选择结果触及瓦片预算且强制最小层级未完成时，整体降低一次最小层级并重新选择，禁止输出半完成覆盖。

### 长期规则

LOD 的第一条不变量是“所有可见地表必须被某个可渲染叶节点或其就绪祖先覆盖”。性能预算只能降低细节，不能制造空洞。

## 6. LOD 达到 14、16 或 20 后不再更新

### 现象

继续放大后，经纬网或瓦片停在某一级；最大缩放看起来被阻尼卡住。

### 根因

- 数据源最大层级、球面几何最大层级和相机距离限制被错误绑定。
- 近地缩放速度直接按地心距离计算；地心距离几乎恒定为地球半径，使灵敏度和阻尼曲线失真。
- 高层级使用绝对地心坐标提交 GPU，float32 精度逐渐达到极限。

### 当前处理

- 球面几何 LOD 支持 2–27；选择器预留到 30。
- Google 示例影像到 20 后使用祖先纹理回退，但几何仍可继续细分。
- 相机灵敏度依据当前方向的真实椭球离地高度，而不是地心距离。
- 高层级采用瓦片局部坐标、局部 ENU 基和高低位相机/原点拆分，减少 GPU 大坐标误差。

### 长期规则

必须分别记录：相机层级、球面几何 LOD、数据源层级、样式层级和数据最大层级。任何一个上限都不能隐式限制其他层级。

## 7. 高层级平移过敏、缩放阻力过大

### 现象

0–6 级旋转平滑，但 6–20 级同样的鼠标移动跨越很大地理范围；7 级以后滚轮阻力过强，难以到达最大层级。

### 根因

OrbitControls 默认按地心尺度工作，不适合从全球到亚米级的连续高度范围。固定 `rotateSpeed` 和 `zoomSpeed` 会在某些高度过快、另一些高度过慢。

### 当前处理

- 旋转速度随椭球离地高度按幂曲线衰减。
- 缩放速度近似与离地高度线性相关，并设置独立的最小速度，避免阻尼降到不可操作。
- 相机最小、最大距离由 LOD 目标像素和当前 viewport 动态反算。

### 长期规则

地球相机最终应替换普通 OrbitControls，直接按屏幕射线与椭球/地形交点执行拖拽，并以“屏幕像素对应地面距离”定义操作灵敏度。

## 8. 矢量制图层级与球面 LOD 不一致

### 现象

- 同一位置矢量道路、地名和卫星影像比例不一致。
- 球面 LOD 15 看起来加载了更高一级矢量制图内容。
- 不同 LOD 混合时，同一视口出现道路密度和文字层次断层。

### 根因

球面 LOD 是几何误差等级，而矢量 zoom 是数据概化和制图规则等级，两者不天然相等。Esri 当前样式与演示球面尺度约有两级偏移，但这个偏移不是所有 MVT 服务的通用常量。

### 当前处理

- 引入 `levelOffset`、`setViewLevel()` 和 `maximumSourceLevel()`。
- 当前 Esri 演示传入 `-1.7`，provider 会把偏移规范化为整数 `-2`，最终使用 `floor(相机层级 - 2)`。
- 整个视口先确定统一的最高数据/样式层级，再让更细球面瓦片裁切复用纹理，避免单个叶节点各自选择不同制图 zoom。
- 矢量模式可用 `minimumLodLevelOffset` 限制过粗球面叶节点；超出瓦片预算时整体降级。

### 长期规则

层级映射必须属于 `DataSource` 配置，可按服务声明、样式 metadata 或校准表设置，禁止在引擎全局硬编码“矢量永远减二”。

## 9. 国家层级标签不足与标签互相遮挡

### 现象

全球尺度缺少国家名称；开启更多标签后，城市、国家和水域名称互相遮挡；跨瓦片处可能重复或被切断。

### 根因

- `En.json` 中部分国家标签图层默认隐藏。
- 当前矢量方案逐瓦片栅格化，碰撞检测只能知道同一张 Canvas 内的标签，无法掌握完整屏幕状态。
- MVT 通常在相邻瓦片复制边缘要素，逐瓦片绘制容易重复。

### 当前处理

- `showCountryLabels` 只覆盖 `Admin0 point` 的隐藏状态。
- 国家、争议区、省级、城市、海洋和水域按优先级排序。
- 单瓦片内执行碰撞检测；越过瓦片安全边界的标签暂不绘制，以减少重复和截断。

### 尚未彻底解决

跨瓦片、跨层级、随相机连续稳定的标签必须进入独立的屏幕空间 SymbolPass。当前 Canvas 标签适合验证样式和 MVT 接入，不是最终标注架构。

## 10. 低 LOD 和斜视角出现黑色三角孔网

### 现象

相机约 1–6 级时，越靠近球体边缘越容易出现黑点、三角孔洞和经纬网/底图曲率不一致；卫星和矢量都会出现。

### 根因

- 所有瓦片固定使用 16 段网格。LOD2 单瓦片横跨 90°，相邻顶点之间的直线弦会下沉到椭球内部。
- LOD2 使用 16 段时，赤道方向单段弦面最大下沉约 7.68 千米。
- 底层遮挡球的粗三角面在某些视角穿出瓦片弦面，最终显示为黑色三角孔洞。
- 固定 8 段的经纬网也无法贴合低层级大跨度曲面。

### 当前处理

- 栅格几何在 LOD2–6 分别使用 256/128/64/32/16 段，LOD2 弦面误差降到约 30 米。
- 细分数量使用二次幂，父边界采样点与两个子瓦片严格对齐，减少 T 形接缝。
- 经纬网在 LOD2–6 分别使用 128/64/32/16/8 段。
- 遮挡球相对参考椭球内缩最多 1000 米，确保它不会穿出地图表面。

### 长期规则

地形接入后，不能继续依赖单纯增加规则网格解决所有接缝。必须同时实现共享边界高度、skirt、父子过渡和几何误差驱动的细分。

## 11. Web Mercator 极区圆孔

### 现象

从北极或南极正上方观看时，±85.05112878°以外出现圆形无瓦片区域。

### 根因

标准 Web Mercator 本身不覆盖极点。这不是缺瓦片，也不能通过请求不存在的 XYZ 行来补齐。

### 当前结论

极区圆孔目前可接受。后续可以选择：

- 用纯色、海洋或低分辨率全球纹理填充极冠；
- 增加极区专用投影和数据源；
- 在支持真实全球数据时切换到 Geographic/Cube/其他全球划分。

非极区出现孔洞则始终视为错误。

## 11.1 GPU 地形位移后出现视口孔洞

### 现象

近地、接近水平观察时，山体已经被 Terrain-RGB 在 GPU 中抬升到视口内，但部分区域仍显示底层暗蓝色遮挡球。

### 根因

不是 ShaderMaterial 的透明、`discard` 或 Three.js 网格视锥裁剪。影像网格已经关闭 `mesh.frustumCulled`，但 CPU 四叉树仍按未变形 WGS84 椭球执行视锥和地平线剔除；本应由地形抬升进入视口的瓦片在生成网格前已经被 LOD 删除。

### 修复

- LOD 接受最大正地表位移边界；
- 曲面包围球同时覆盖参考椭球与最大抬升面；
- 地平线剔除加入高程带来的额外可见角；
- 远景新增瓦片仍使用地平线屏幕误差降级，由完整低级父瓦片覆盖；
- 地形关闭时移除位移扩展并清空包围体缓存。

长期应由每个地形节点提供实际 `minimumHeight/maximumHeight`，替代全局保守高度。

## 11.2 斜视瓦片长期打满与地形高差裂缝

### 根因

- 所有节点统一使用全球最大地形高度扩展视锥和地平线，普通丘陵也按珠峰级高度保守处理，45°以上斜视容易持续触及瓦片预算。
- 相邻叶节点可能分别使用目标 Terrain-RGB 和不同级祖先回退，公共边顶点从不同高度纹理采样，形成真实几何高差。

### 修复

- TerrainTileLayer 向 LOD 暴露已加载祖先的真实 `maximumHeight`，未知节点才使用全局安全上限；
- 高度数据 revision 改变时使 LOD 包围体缓存失效；
- 地形队列除目标瓦片外固定请求其直接父级，作为回退与接边基准；
- 目标高度在源纹理边缘两个 texel 内渐变到父级高度；
- 影像地形网格四周增加随 LOD 缩放的向下裙边，遮挡残余量化裂缝。

## 11.3 右键旋转后天际线倾斜甚至竖直

现象：接近水平观察时长按右键旋转，地表会绕视线滚转，天际线不再水平；继续操作后很难恢复正常姿态。

根因：旧实现把相机已有的 `up` 向量随旋转四元数持续累乘，相当于允许航向、俯仰和滚转三个自由度同时变化。误差在长时间拖动后积累，且 `flyTo` 曾允许初始化到交互倾角上限之外。

处理：

- 航向始终绕焦点处的椭球法线旋转，俯仰始终绕该处切线轴旋转；
- 每次姿态更新后，把焦点地表法线投影到相机视平面，将其作为屏幕上方向并重建相机姿态，强制滚转角为零；
- 正视地表时投影可能退化，此时使用旋转前的上方向或局部北向作为稳定后备；
- `flyTo`、右键旋转和中键俯仰共用相同的倾角范围，防止相机从非法姿态开始交互。
- 倾角钳制增加角度死区；到达边界且实际位移为零时清除对应阻尼速度，不再把该帧报告为相机变化；
- LOD 选择缓存上一帧结果，仅在相机姿态、视口、最小层级或地形边界真正变化时重算；Terrain-RGB 连续到达产生的 revision 以 100ms 静默窗口合并，避免边界瓦片逐请求闪烁。
- 正俯视是上方向投影为零的相机奇点。俯仰限制必须在旋转前钳制有符号增量，不能用旋转后的无符号夹角判断，否则相机会穿过正俯视点并产生 180° 翻转；
- 正俯视附近使用连续传递的相机上方向；离开奇点时动态选择与该上方向一致的切线轴符号。叉积退化检测必须在归一化之前完成。

结论：地球观察相机不是自由飞行相机。产品默认模式应保留航向与俯仰两个自由度，并以局部地平面约束滚转，才能保证稳定的水平天际线。

## 11.4 长时间加载后 WebGL 上下文丢失

现象：瓦片较多或长时间旋转缩放后出现 `CONTEXT_LOST_WEBGL`；恢复后偶发
`delete: object does not belong to this context`，同时动画帧持续超时。

根因不是单一 shader 错误，而是资源峰值和每帧资源抖动叠加：

- 影像缓存只有张数限制。512×512 RGBA 纹理连同 mipmap 约占 1.33 MiB，数百张即可消耗数百 MiB 显存；
- Terrain-RGB 同时保留 CPU `Float32Array` 和 GPU R32F 纹理，原先没有字节预算；
- 经纬网每次 LOD 或地形 revision 都替换四个 BufferAttribute，旧 WebGLBuffer 不能及时回收；
- 相机静止时仍每帧对数百瓦片排序、同步材质和检查缓存；
- 高 DPR 大窗口的颜色、深度和抗锯齿缓冲会额外形成很大的瞬时显存占用；
- 上下文丢失期间异步请求仍可能完成，若按正常路径删除旧上下文对象，恢复后会触发跨上下文删除错误。

已完成处理：

- 影像加入 `maxTextureBytes` 硬预算并公开实时 MiB；只保护当前真正绑定的回退/目标纹理，未绑定候选允许 LRU 淘汰；
- 地形加入 CPU 高程场 + GPU 纹理的 `maxResourceBytes` 联合预算；
- 经纬网 BufferAttribute 改为按 2 的幂扩容并稳定复用，扩容前显式释放旧 geometry；地形响应按静默窗口合并刷新；
- 影像和地形在同一 LOD 选择、且没有新资源到达时跳过排序和材质同步；
- drawing buffer 默认限制为 800 万像素，4K/高 DPR 窗口自动降低实际 DPR；
- 监听 `webglcontextlost/restored`：丢失时暂停请求泵和渲染，不删除失效上下文对象；恢复后重新标记纹理、材质和属性上传，并强制重建一次 LOD；
- 无祖先覆盖的桥接纹理优先于高清锐化，预算受限时先保证完整画面。

长期规则：缓存上限必须同时受“数量、CPU 字节、GPU 字节”约束；资源释放必须属于创建它的上下文代次。后续 `RequestScheduler` 和 `TileStateMachine` 要正式引入 context generation、上传队列预算与可取消任务，不能依赖各图层的临时布尔状态。

### 11.5 影像长期停留在祖先层级与高层级拖拽失效

- 影像显存达到硬预算时，如果当前绑定的低级祖先全部受保护，旧请求泵会禁止目标纹理进入，形成“必须先加载目标才能释放祖先、必须先释放祖先才能加载目标”的升级死锁。处理方式是为并发目标保留有限过渡字节；目标绑定后再淘汰祖先，同时对可见失败请求执行指数退避重试。
- 调试状态必须同时显示“目标数据层级”和“实际绑定层级”，只显示 Provider 计算层级无法判断请求、解码、上传或材质绑定卡在哪一步。
- 左键拖拽旧公式把高度比例不断压到零，并用固定角速度阈值过滤输入，因此 18 级后小位移被吞掉、累计多次才偶尔生效。现改为根据相机高度、FOV 和视口高度计算地面米/像素，再换算球心旋转角；13–18 级跨度降低，高层级仍保持连续响应。

### 11.6 Terrain-RGB 跨瓦片真实几何裂缝

父级边缘渐变和 skirt 只能遮挡裂缝，不能保证公共边顶点高度相等；低仰角、浅色矢量底图和较大地形高差下仍会暴露。另一个底层原因是常见 Terrain-RGB 为 256×256 采样，即每边只有255个间隔，无法在四叉树父子层级间严格二分对齐。

处理：

- Terrain-RGB 解码后重采样为257×257，即256个可二分单元；
- 新瓦片到达时只处理受影响的四条边及向内4个 texel，不重算整张高度场；
- 同级公共边按相同地理位置计算平均高度；
- 跨级公共边以粗级可表达曲线为权威，细级边界单向贴合该公共曲线；禁止粗细两侧对称取平均，否则两侧会同时向中值隆起，形成“M形”接边墙；
- 暂无相邻细瓦片时，细瓦片先贴合当前可用祖先；邻居到达后两侧纹理同时标记更新；
- shader 不再用本瓦片父级覆盖已经协调过的公共边，skirt 仅作为异步切换期间的最后保险。
- 边界向内过渡不能固定为少量 texel；根据纠正高差、纬度和当前米/texel估算所需宽度，以最大附加坡度约束在6–128 texel内平滑；不仅统一边界高度（C0），还要用 Hermite 过渡统一两侧法向坡度（C1），否则低视角会形成 V 形折线；
- 257×257 高程场是256个单元的端点采样，GPU读取时必须把逻辑UV映射到 texel center，尤其父级子区域不能直接使用原始归一化UV；常规内部边不再生成向下裙边，避免暂态误差被显示成蓝色竖墙。
- 接边后的 `minimumHeight/maximumHeight` 必须从当前瓦片实际高程数组重新统计，不能合并邻居整张瓦片的极值；否则高山极值会沿接边关系传播，使城市瓦片的LOD包围体虚增并持续撞到瓦片预算上限。平滑结果同时限制在原始带与目标边界的局部高度包络内，禁止Hermite过冲重新制造M形墙。

合成高程测试必须保证：同级边最大误差为0、跨级所有重合采样点误差为0、缺邻居时细瓦片四边与祖先一致。

## 12. Git 提交与代码恢复问题

### 现象

- 重构代码曾因错误保存或覆盖而丢失。
- `git pull --tags origin main` 报 `Failed to connect to github.com port 443`。

### 根因

- 没有在重构期间保留独立分支和小步提交，工作区损坏后缺少可靠恢复点。
- GitHub 443 报错是网络连接失败，不是本地代码或 commit 内容错误。

### 长期规则

- 每个可运行阶段建立本地 commit；高风险重构使用独立分支。
- 修改前检查 `git status`，不覆盖来源不明的工作区变更。
- pull/push 失败时先保存本地 commit，再检查代理、DNS、HTTPS 网络和远端地址。
- 禁止用 `reset --hard` 处理尚未确认归属的修改。

## 13. 当前架构风险、既定决策与技术债

### 13.1 把 MVT 输入误认为原生矢量渲染

现象：`VectorStyleTileProvider` 请求和解析 MVT，但最终绘制为 CanvasTexture，容易因为输入是 PBF 而被称为“真正矢量”。

根因：数据格式与渲染产物没有分层命名。当前要素在 Canvas 2D 中栅格化后已经失去单要素 GPU 几何，无法直接拾取、动态编辑，也无法执行完整跨瓦片标签布局。

已完成处理：

- 当前路径长期保留并正式实现为 `ArcGisVectorRasterProvider`，未来图层类型为 `rasterized-vector`；
- 已拆分 `MvtTileSource`、`MvtDecoder`、`ArcGisStyleAdapter`、`CanvasVectorRasterizer` 和组合 Provider；
- source、decoder、style adapter 不依赖 Canvas，可被未来 GPU VectorLayer 复用；
- `VectorStyleTileProvider` 作为标记过时的兼容入口委托新 Provider，不再承担请求、解码、样式和绘制实现；
- Canvas 路径负责 ArcGIS 服务快速兼容、复杂样式降级和效果对照，不承担点线面编辑目标。

### 13.2 每个图层各自维护请求与状态

现象：影像、地形和矢量 Provider 分别维护请求队列、并发和缓存。继续直接增加 GPU 矢量会产生第四套相似实现。

风险：

- 同一个 source/tile 被多个图层重复请求和解码；
- 相机快速移动时无法统一取消与重排；
- 父瓦片过早退出、子瓦片乱序到达或失败时出现闪烁和孔洞；
- 网络、Worker、GPU 上传混成一个 loading 状态，无法限流和诊断；
- 缓存只按数量而非字节控制，长期运行不可预测。

既定处理顺序：

```text
拆分并保留 ArcGIS Canvas 兼容路径
  → TileStateMachine
  → RequestScheduler
  → LayerCollection + DataSourceRegistry
  → 原生 GPU VectorLayer
```

`TileStateMachine` 必须按“空间瓦片 + 内容类型 + source + variant”记录状态，使同一空间节点的影像、地形、栅格化矢量和原生矢量可以独立处于 queued/loading/decoding/uploading/ready/failed/cancelled。只有子内容形成完整覆盖后才能原子替换父内容。

`RequestScheduler` 必须统一网络、Worker 解码和 GPU 上传队列，提供动态优先级、去重、取消、并发限制、重试冷却和按字节缓存预算。没有祖先覆盖的桥接任务优先；已经有覆盖时，高层级、屏幕误差大且靠近视口中心的目标任务优先。

`LayerCollection` 负责图层顺序、显隐、透明度、层级范围、底图互斥组、生命周期和工程序列化；共享缓存归 source/scheduler 所有，删除图层不能清空其他图层仍在使用的资源。

### 13.3 仍需跟踪的能力缺口

- `VectorStyleTileProvider` 仅实现 Mapbox Style v8 的一部分：background、fill、line、基础 symbol、有限 filter/expression。
- 当前只有一个 `RasterTileLayer`，地图切换仍通过替换 provider 完成，尚未实现多服务同时叠加。
- 已有只读 Terrain-RGB、高度采样、GPU 位移、父级接边和 skirt；尚缺逐节点几何误差、完整 geomorph、真实地形拾取、地形编辑和撤销重做。
- 尚无原生 GPU fill/line/symbol、FeatureIndex、跨瓦片 SymbolPass 和点线面编辑。
- 尚无 MBTiles/PMTiles、Quantized-Mesh 和 3D Tiles 适配器。
- WebGL shader 必须加入真实浏览器回归测试；构建和类型检查无法覆盖 GPU 驱动差异。

## 14. 每次核心修改后的最低验证清单

1. 运行 `npm run typecheck` 和 `npm run build`。
2. 检查浏览器控制台没有 shader、纹理、CORS 和未处理 Promise 错误。
3. 在卫星和矢量模式分别测试相机 2、3.6、6、15、20、22、27 附近。
4. 测试正视、斜视、地平线边缘、赤道、日期变更线和接近南北极的角度。
5. 改变浏览器宽高比和 DPR，确认视口边缘没有缺瓦片。
6. 断网或模拟慢网，确认祖先回退持续覆盖，不出现透明洞。
7. 快速旋转和缩放，确认请求能取消或降级，瓦片预算不会长期打满。
8. 验证父子 LOD 交界、瓦片边界、文字重复和样式层级连续性。
