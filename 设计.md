我先按“能否支撑一个真实 3D 地球引擎”来审查你上传的代码，不只看能不能跑 demo，也会看后续海量瓦片、地形、相机和编辑能力的风险。

已经看出一个核心方向：它现在更像“用 Three.js 验证球面地形瓦片”的 demo，不是完整 GIS 地球引擎。最明显的风险集中在坐标精度、LOD、地形接缝、相机深度精度和数据管理。

我还发现它把“平面瓦片”和“球面地形”两套逻辑放在一起：PlanarMapTileLayer 是平面 Web Mercator 瓦片，TerrainLayer 又在球面上贴地形。这个混合架构前期方便验证，后期会拖累统一坐标、拾取、量测和编辑。

已思考 2m 25s

我看了你上传的代码。结论是：Three.js 可以做 3D 地球显示，但这个项目当前更像“球面地形瓦片验证 demo”，还不是一个真正的 GIS 地球引擎。index.html 里标题和 HUD 都还是 demo 定位 。最大问题不是 Three 本身，而是：地理坐标、LOD、地形、瓦片调度、拾取量测、海量数据编辑这些 GIS 核心能力还没形成体系。

主要问题
1. 架构上混用了“平面地图”和“球面地球”

代码里同时存在：

PlanarMapTileLayer.ts：平面 Web Mercator 瓦片逻辑。
TerrainLayer.ts：球面地形瓦片逻辑。
PlanarValidation.ts：用一个球体 proxy 加地形层来验证。

这说明项目现在处于验证阶段。问题是后面如果继续这样发展，会出现两套坐标、两套瓦片、两套拾取、两套 LOD。对于 GIS 编辑软件来说，这会非常麻烦。

你最好尽早明确主线：

要做真 3D 地球，就以 ECEF/球面四叉树为主。
平面地图只能作为局部投影模式，不要让它和地球模式平级混杂。

2. 深度精度风险很大

Engine.ts 里相机默认：

cameraNear = 10
cameraFar = 80_000_000

这个 near/far 比例太夸张，地球尺度下很容易出现：

地形闪烁、瓦片缝隙、线面重叠抖动、模型穿插、远近物体 z-fighting。

Three.js 是有 logarithmicDepthBuffer 和 reversedDepthBuffer 这些深度缓冲相关选项的，但默认不开，需要你在 renderer 初始化时明确设计；官方文档也说明这些选项默认是 false。

这类问题在做地球时非常关键。Cesium 这类引擎之所以复杂，很大一部分就是在解决地球尺度精度问题。

3. floating origin 只是有雏形，还没完整用起来

Engine.ts 里有：

setRenderOrigin()
rebaseRenderOriginToCamera()
worldToRenderPosition()
renderToWorldPosition()

说明你已经考虑到了渲染原点偏移，这是对的。

但现在看，PlanarValidation 初始化时只是：

this._setRenderOrigin({ x: 0, y: 0, z: 0 }, false);

没有看到按相机位置动态 rebase 的完整策略。

地球半径是几百万米级，如果直接把世界坐标丢给 GPU，Three/ WebGL 最终顶点精度还是 float32，近地编辑时会抖。你需要明确一套：

CPU：双精度世界坐标
GPU：局部坐标
相机附近：动态 floating origin
数据瓦片：按局部 origin 转换后渲染

否则后面做 1 米、厘米级编辑会出问题。

4. 地形 LOD 太弱，最多只能算演示级

TerrainLayer.ts 里 maxZoom 被限制到 13：

this._minZoom = clampInt(options?.minZoom ?? 0, 0, 13);
this._maxZoom = clampInt(options?.maxZoom ?? 13, this._minZoom, 13);

main.ts 里也设置了：

maxZoom: 13
tileSegments: 48
tileRadius: 2

这对“地球预览”可以，但对 GIS 软件不够。

大概问题是：

z13 影像/地形精度不够近地编辑；
每个瓦片只有 48 段，地形细节会被网格限制；
没有基于屏幕误差 SSE 的 LOD；
没有父子瓦片平滑替换；
没有地形裙边 skirt；
没有 geomorphing；
没有真正的视锥裁剪和地平线裁剪。

而且 TerrainLayer 里瓦片 mesh.frustumCulled = false，这意味着 Three 不会自动剔除视锥外瓦片。小 demo 没问题，瓦片数量一上来就是性能坑。

5. 地形高度只在 GPU shader 里做，GIS 功能会失真

TerrainLayer.ts 里地形顶点先按球面生成：

const world = this._geo.wgs84ToThree(lat, lon, 0);

然后在 shader 里通过 Terrain RGB 贴图做位移：

transformed += normalize(position) * sagHeightUnits;

这有一个很大的 GIS 问题：CPU 侧并不知道真实地形高度。

后果是：

鼠标拾取可能拾到椭球面，不是真实地形面；
量测高度不准；
坡度、坡向、剖面分析做不了；
水流、水淹、视域分析做不了；
地形编辑没法落到数据结构上；
模型贴地会漂浮或下沉。

这对普通 3D 可视化可以接受，但对你想做的 GIS/地球引擎不够。你需要 CPU/Worker 侧解码 DEM，生成真实网格、法线、包围盒、空间索引，同时 GPU 只负责渲染。

6. 地形法线会不准

现在 geometry 是先：

geometry.computeVertexNormals();

然后 shader 再把顶点按高度抬起来。

这意味着法线是在“未抬高的球面”上算的，不是基于真实地形起伏算的。结果就是山体光照不真实，坡面明暗不对。

解决方式：

CPU 生成真实高度网格后计算法线；
或者提供 terrain normal texture；
或者 shader 中根据高度纹理采样邻域估算法线。

7. 瓦片服务不能直接这样用于生产

main.ts 里直接写了 GeoVisEarth 的 token，并且影像用的是：

https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png

这有两个风险。

第一，token 不能硬编码在前端，任何人都能在浏览器里看到。应该走后端代理、签名、短期 token 或自有瓦片服务。

第二，OSM 官方瓦片服务不适合作为你的生产瓦片源，尤其不能做批量预取、离线缓存、大范围高 zoom 扫描。OSM 官方 tile policy 明确禁止 bulk downloading / scraping / offline use。

真正做产品，要准备：

自建瓦片服务；
本地 MBTiles / PMTiles / GeoPackage；
商业影像源；
缓存策略；
请求限流；
离线包管理。

8. 相机控制还是普通 Orbit，不是地球相机

CameraController.ts 现在是围绕一个 target 做球坐标旋转、平移、缩放，本质接近 OrbitControls。

这对 demo 可以，但地球相机还需要：

贴地飞行；
绕地球惯性旋转；
避免穿透地球；
自动根据高度调整 near/far；
根据经纬度定位；
支持地形碰撞；
支持地下/海底模式；
支持 2D/2.5D/3D 切换；
支持倾斜摄影、模型、点云的相机约束。

现在 PlanarValidation 里有 enforceCameraOutsideGlobe()，只能算初步防穿地，还不是完整地球相机系统。

9. 海量 GIS 数据编辑能力完全没体现

这个项目现在主要在做：

地球球体；
地形 RGB；
影像瓦片；
LOD 网格；
基础相机；
debug HUD。

但你真正想做的是类似“春秋 GIS / MyEarth”这种桌面 GIS 地球引擎，那还缺：

矢量瓦片；
点线面渲染；
千万级要素调度；
属性表；
空间索引；
拓扑编辑；
捕捉 snapping；
撤销重做；
图层样式系统；
符号系统；
标注系统；
3D Tiles / I3S / 点云；
本地数据源；
编辑后 dirty tile 更新；
投影转换；
坐标系统管理。

所以当前项目离“可编辑 GIS 引擎”还有很远。

最核心的判断

Three.js 不是不能用，但不能把 Three.js 当 GIS 引擎。

Three 只能负责：

场景
相机
Mesh
材质
纹理
shader
渲染循环

你还必须自己实现：

地球坐标系统
瓦片四叉树
LOD
地形数据结构
影像调度
矢量调度
空间索引
拾取量测
编辑系统
缓存系统
数据源系统

这部分工作量接近“自己写一个小 Cesium”。

我建议你这样改方向
如果目标只是 3D 地球展示

可以继续基于 Three 做，但先补这几项：

把 TerrainLayer 改成真正四叉树 LOD。
增加父子瓦片渐进替换。
地形加 skirt，解决缝隙。
CPU/Worker 解码 DEM，不要只在 shader 里抬高。
增加动态 floating origin。
调整 near/far，必要时启用 reverse depth 或 logarithmic depth。
不要直接用 OSM 和明文 token。
做统一的 TileProvider 抽象。
如果目标是桌面 GIS 地球引擎

那 Three.js 只能当原型。真正长期路线建议是：

C# / C++ / Rust / Native 渲染核心
+
自研瓦片调度和数据编辑层
+
Python 做地理处理和批处理

Three 可以保留做 Web 预览、轻量演示、原型验证，但不要把它当最终核心引擎。

一句话评价：这个项目现在方向是对的，已经碰到地球引擎的核心问题了；但当前代码还是“渲染 demo”，不是“GIS 地球内核”。如果继续沿这个结构堆功能，后期会被坐标、LOD、地形拾取和海量数据编辑卡死。

他初衷是2d3d切换方便，如果要兼容2d3d底图，是分开做引擎还是一起做，建立强制转化关系？

这其实是架构分层问题，不是简单“2D/3D 共用一套坐标”就能解决。我会按 GIS 产品长期维护的角度来判断：哪些应该共用，哪些必须分开。

已思考 25s

不要做两个完全独立的引擎，也不要把 2D/3D 强行互相转换。

最合理的是：

一个 GIS 核心内核
+
两个视图/渲染模式

也就是：

CoreGIS
 ├─ 数据源管理
 ├─ 图层管理
 ├─ 坐标转换
 ├─ 瓦片调度
 ├─ 缓存系统
 ├─ 拾取/查询/编辑逻辑
 ├─ 样式系统
 │
 ├─ MapView2D 平面视图
 └─ GlobeView3D 球面视图
1. 不建议做两个完全分开的引擎

如果 2D 和 3D 分成两个独立引擎，前期看起来简单，后期会出现一堆重复系统：

2D 一套餐图层管理
3D 一套餐图层管理

2D 一套瓦片缓存
3D 一套瓦片缓存

2D 一套拾取
3D 一套拾取

2D 一套坐标转换
3D 一套坐标转换

2D 一套编辑
3D 一套编辑

最后会变成维护噩梦。

尤其你这个项目不是单纯展示地球，而是要往 GIS 软件、编辑、海量数据方向走。数据层、图层层、缓存层、坐标层必须统一。

2. 也不建议“强制 2D 转 3D”或“强制 3D 转 2D”

这个思路也容易出问题。

比如强制把 2D 平面底图贴到球面上，或者强制把 3D 地球压平成 2D，看似统一，其实会导致：

2D 地图投影不自然
3D 地球瓦片接缝难处理
拾取坐标混乱
相机切换困难
标注碰撞不一致
量测结果不可靠
局部投影和全球投影冲突

2D 和 3D 的数学基础本来就不同：

2D：投影平面坐标，例如 WebMercator、CGCS2000、高斯投影
3D：椭球体/ECEF/球面坐标

所以不能要求它们在渲染层完全统一。

3. 推荐方案：统一“地理语义”，分离“渲染表达”

核心原则是：

数据统一
坐标统一
图层统一
瓦片源统一
渲染方式分开

也就是同一个图层，可以在 2D 和 3D 下有不同渲染器。

例如影像底图：

ImageTileLayer
 ├─ 数据源：XYZ / WMTS / TMS
 ├─ 缓存：统一
 ├─ 请求队列：统一
 │
 ├─ 2D Renderer：瓦片铺在平面投影坐标上
 └─ 3D Renderer：瓦片贴到球面/地形表面上

矢量图层也是一样：

VectorLayer
 ├─ 数据源：Shapefile / GeoJSON / MVT / GDB / PostGIS
 ├─ 样式：统一
 ├─ 属性：统一
 │
 ├─ 2D Renderer：平面线、面、符号、标注
 └─ 3D Renderer：贴地线、立体面、挤出、billboard、3D 标注

这样 2D/3D 可以共享数据，但不用强行共用同一套 mesh。

4. 应该建立“标准地理坐标中间层”

这个非常关键。

不要让 2D 和 3D 直接互相转，而是都转到一个统一中间表达。

建议内部标准用：

LonLatHeight
经度、纬度、高程

或者更底层用：

ECEF
地心地固坐标

结构大概是：

数据源坐标
  ↓
CRS 转换
  ↓
统一地理坐标 Lon/Lat/Height
  ↓
根据视图转换
      ├─ 2D：投影到平面 XY
      └─ 3D：转换到 ECEF/球面/地形表面

这样不是 2D 转 3D，也不是 3D 转 2D，而是：

源数据 → 统一地理坐标 → 当前视图坐标

这个关系才是健康的。

5. 2D/3D 切换时，只转换“视图状态”，不要转换全部数据

切换时不要把所有图层数据重算一遍。应该只转换相机/视图状态。

统一保存一个 ViewState：

type ViewState = {
  centerLon: number;
  centerLat: number;
  height?: number;
  zoom?: number;
  scale?: number;
  heading: number;
  pitch: number;
  roll?: number;
};

2D 模式下：

centerLon/Lat → 平面中心点
zoom/scale → 地图比例尺
heading → 旋转角
pitch = 0

3D 模式下：

centerLon/Lat → 地球焦点
height → 相机高度
heading → 朝向
pitch → 俯仰角

切换逻辑：

2D → 3D：
当前地图中心点、缩放级别、旋转角
转换成地球相机高度、焦点、heading、pitch

3D → 2D：
当前地球焦点、相机高度、heading
转换成地图中心、zoom、rotation

这样切换会自然很多。

6. 底图可以共用数据源，但不能完全共用渲染

比如同一个 XYZ 瓦片源：

https://server/{z}/{x}/{y}.png

在 2D 里是一个矩形瓦片：

PlaneGeometry

在 3D 里应该变成球面瓦片：

CurvedTileGeometry / EllipsoidTileMesh

也就是说：

瓦片地址、缓存、请求可以共用
瓦片 mesh 不能强行共用

这点非常重要。

你现在项目里的 PlanarMapTileLayer 和 TerrainLayer 其实已经暴露出这个问题了：一个是平面瓦片，一个是球面地形瓦片。正确方向不是把它们硬合成一个类，而是抽象成：

TileProvider
 ├─ getTileUrl()
 ├─ loadTile()
 ├─ cacheTile()
 │
TileRenderer2D
 ├─ createPlaneTileMesh()
 │
TileRenderer3D
 ├─ createGlobeTileMesh()
7. 当前项目建议这样调整

你现在不应该继续叫 PlanarValidation 这种验证类作为主架构。建议改成：

Engine
 ├─ SceneManager
 ├─ LayerManager
 ├─ DataSourceManager
 ├─ CRSManager
 ├─ TileScheduler
 ├─ ViewManager
 │    ├─ MapView2D
 │    └─ GlobeView3D

然后图层不要直接绑定 2D 或 3D，而是这样：

class ImageryLayer {
  provider: TileProvider;
  renderer2D: ImageryRenderer2D;
  renderer3D: ImageryRenderer3D;
}

切换视图时：

viewer.setViewMode("2D");
viewer.setViewMode("3D");

内部做：

隐藏当前 ViewRenderer
保留 Layer 数据源
转换 ViewState
启用对应渲染器
复用瓦片缓存
8. 哪些必须共用，哪些应该分开？
必须共用
图层管理
数据源管理
瓦片请求
缓存
坐标转换
属性查询
空间查询
编辑事务
样式配置
权限/工程配置
应该分开
相机控制
瓦片 mesh 生成
标注碰撞
地形贴合
深度处理
视锥裁剪
LOD 判断
拾取射线算法
可以部分共用
瓦片编号
瓦片缓存
图层样式
要素过滤
符号定义
选择集
测量结果
9. 最推荐的总体架构

我建议你采用这个方案：

不是两个引擎
不是强制转换
而是一个 GIS Core + 两个 View Adapter

完整一点：

SpringAndAutumnGIS / MyEarth Engine

Core
 ├─ CRS / Projection
 ├─ Layer
 ├─ DataSource
 ├─ TileProvider
 ├─ TileCache
 ├─ FeatureStore
 ├─ Style
 ├─ Selection
 ├─ EditSession
 └─ Analysis

View
 ├─ MapView2D
 │   ├─ 2D Camera
 │   ├─ Plane Tile Renderer
 │   ├─ 2D Label Renderer
 │   └─ 2D Picking
 │
 └─ GlobeView3D
     ├─ Globe Camera
     ├─ Ellipsoid Tile Renderer
     ├─ Terrain Renderer
     ├─ 3D Label Renderer
     └─ 3D Picking

这样后面你想支持：

二维地图
三维地球
局部场景
海底地形
倾斜摄影
点云
矢量编辑
栅格分析

都不会互相拖死。

我的判断

2D/3D 不要分成两个完全独立引擎，但渲染层必须分开。

最准确的设计是：

一套 GIS 内核
两套视图渲染器
一个统一坐标中间层
一个统一图层/数据/缓存系统
切换时只转换 ViewState，不强制转换全部数据

你现在这个项目的初衷“2D/3D 切换方便”是对的，但实现上不能靠 PlanarMapTileLayer + TerrainLayer 混在一个验证类里硬切。应该尽早把它升级成 Core + MapView2D + GlobeView3D 的结构。