# SpringAndAutumnGIS 核心设计

> 版本：重构阶段 Core 01
>
> 最后更新：2026-08-02
>
> 目标：以当前 WGS84 三维地球和屏幕空间 LOD 为稳定底座，逐步支持地形渲染、地形编辑、多服务图层、原生矢量渲染、点线面编辑与 MBTiles，而不破坏坐标精度和可见地表覆盖。

## 1. 产品方向

SpringAndAutumnGIS 不是“在 Three.js 球体上贴几张图”的展示程序，而是一个可扩展的轻量 GIS 内核。Three.js 负责 WebGL 场景、材质、资源对象和最终绘制；GIS 内核负责：

- 坐标参考系与高精度坐标；
- 地球/平面空间划分；
- 四叉树 LOD 与可见性；
- 数据源、图层、请求、解码和缓存；
- 影像、地形、矢量、标注和编辑数据；
- 拾取、查询、编辑事务与持久化。

长期采用“一套 GIS Core + 多种 View Renderer”，而不是两个完全独立的 2D/3D 引擎。数据、图层、样式、缓存、选择集和编辑事务应共享；相机、投影、网格、深度、标注布局和拾取实现按 2D/3D 视图分离。

## 2. 不可破坏的核心不变量

### 2.1 坐标不变量

1. CPU 世界坐标使用 JavaScript double，权威地理表达为 `longitude/latitude/height + CRS`。
2. WGS84 三维世界轴固定为：`Y` 北极、`+Z` 零经度、`+X` 东经 90°。
3. GPU 不直接依赖数百万米绝对 float32；使用相机高低位、瓦片原点高低位和瓦片局部坐标。
4. 数据坐标只能走“源 CRS → 权威地理坐标/ECEF → 当前视图坐标”，禁止 2D 与 3D mesh 互相强制转换。
5. 度、弧度、米和瓦片局部单位必须在类型/API 边界明确，不允许隐式混用。

### 2.2 覆盖不变量

任意相机、窗口尺寸和网络状态下，每一块可见地表必须由以下之一覆盖：

- 当前选择的就绪叶瓦片；
- 叶瓦片的就绪祖先资源；
- 明确定义的无数据/极冠回退表面。

`maxTiles`、请求失败和细节降级只能降低清晰度，不能产生透明孔洞。父瓦片只有在所有需要显示的子瓦片均可替代时才能退出。

### 2.3 层级分离不变量

系统必须同时区分以下层级：

| 名称 | 含义 | 当前例子 |
|---|---|---|
| 相机层级 | 由高度、FOV、viewport 和目标像素反算的连续尺度 | `3.6`、`15.2` |
| 球面几何 LOD | 四叉树叶节点的整数几何细分等级 | `2–27` |
| 数据层级 | XYZ/MVT/DEM 服务实际请求的 z | Google 通常与 LOD 对齐；Esri 当前约低 2 级 |
| 样式层级 | Mapbox Style 的 minzoom/maxzoom/expression 输入 | 当前与选定 MVT 数据层级一致 |
| 数据可用上限 | 某数据源真正提供的最大 z | 当前 Google/Esri 示例为 20 |

这些值可以建立显式映射，但不能互相冒充。球面 LOD 27 不代表服务必须存在 z27；数据到达上限后应使用祖先数据覆盖更细几何。

## 3. 当前已实现的基础

### 3.1 坐标与空间划分

- `Ellipsoid`：WGS84 经纬高与引擎 ECEF 风格世界坐标。
- `CoordinateTransform`：地理坐标、世界坐标、Web Mercator、XYZ 和 ENU 局部坐标框架。
- `WebMercatorTilingScheme`：标准 XYZ 四叉树，覆盖 ±85.05112878°。
- `GeographicTilingScheme`：经纬度四叉树备用实现。

当前三维地图主线使用 Web Mercator 四叉树，以便卫星 XYZ、MVT 和未来常规 MBTiles 共用瓦片编号。Web Mercator 极冠不在其定义域内，必须单独回退或引入极区投影。

### 3.2 LOD 选择器

`GlobeLodSelector` 只负责空间可见性和几何细节，不知道网络、纹理、MVT 或地形格式。输入是相机、viewport 高度和可选最小层级，输出唯一的 `SelectedTile[]`。

当前流程：

1. 从 tiling scheme 根节点开始。
2. 使用椭球地平线剔除背面瓦片。
3. 使用保守曲面包围球与完整相机视锥相交，保留所有局部进入视口的瓦片。
4. 根据瓦片世界跨度、相机距离和焦距像素估算基础屏幕误差；再用瓦片局部地表法线与“地表指向相机”的夹角计算掠射权重。正下方权重接近 1，几何地平线附近逐渐降低到配置下限。
5. 从视口中心、下半区和左右边缘发射地表采样射线，确保近水平观察时真正进入视口的前景仍参与高精度误差计算，避免仅使用瓦片中心造成突变或错误降级。
6. 优先拆分屏幕误差最大的节点；远方由完整低层级父瓦片覆盖，不通过删除叶节点降载，因此不会产生孔洞。
7. 使用 `collapseFactor` 形成拆分/合并迟滞，降低层级抖动。
8. 达到 `maxTiles` 时停止提高细节，但不得破坏覆盖。

当前演示参数：

```text
minLevel       = 2
maxLevel       = 27
targetPixels   = 128
collapseFactor = 0.7
maxTiles       = 480
地平线细节下限 = 0.08
地平线过渡指数 = 0.5
```

`minLevel=2` 是当前全球展示和服务数据的产品限制，不应被理解为 LOD 算法只能从 2 开始。以后如果支持 z0/z1，必须先定义极冠、低层级纹理和曲面精度策略。

### 3.3 球面网格与精度

影像瓦片顶点只保存 `[0,1]` 参数坐标。顶点着色器根据瓦片经度、Mercator 行范围和 WGS84 椭球生成真实球面位置。

低层级瓦片跨度很大，固定 16 段会产生公里级弦面误差。当前采用自适应细分：

| 球面 LOD | 影像网格段数 | 经纬网段数 |
|---:|---:|---:|
| 2 | 256 | 128 |
| 3 | 128 | 64 |
| 4 | 64 | 32 |
| 5 | 32 | 16 |
| 6 及以上 | 16 | 8 |

段数保持二次幂，使父瓦片边界采样与两个子瓦片对齐。遮挡底球内缩最多 1000 米，避免底球粗三角面在地平线附近穿出地图表面。

高层级瓦片从 18 级开始使用局部 ENU 近似和高低位坐标拆分，避免 float32 绝对坐标造成经纬网弯曲或抖动。未来地形与矢量顶点必须复用同一高精度坐标模块。

### 3.4 栅格瓦片层

`RasterTileLayer` 消费 `SelectedTile[]`，当前提供：

- XYZ 纹理请求；
- 可配置并发；
- 内存缓存；
- 祖先瓦片回退和 UV 裁切；
- 数据层级偏移；
- 影像与更细球面几何解耦；
- provider 热切换。

当前地图切换是把同一个 RasterTileLayer 的 provider 在 Google 卫星和 Esri 矢量栅格化纹理之间替换。它适合演示，但不是最终多图层方案。

### 3.5 当前矢量瓦片验证方案

`VectorStyleTileProvider` 当前执行：

```text
Mapbox Style v8
  → 选择 vector source
  → 请求并解析 MVT/PBF
  → 按样式绘制到 512×512 Canvas
  → CanvasTexture
  → 交给 RasterTileLayer 贴到球面
```

已支持的验证能力包括：

- background、fill、line 和基础 text symbol；
- 常见比较/filter；
- 少量 zoom/property expression 和 stops；
- 基础颜色、透明度、线宽、虚线、文字 halo；
- 单瓦片标签碰撞和边界安全区；
- 国家、省、市、水域标签优先级；
- 可选恢复样式中隐藏的国家标签。

当前 Esri 演示的数据映射约为：

```text
数据/样式层级 = floor(相机层级 - 约 2)
```

这只是当前服务校准值。其他 MVT 或 MBTiles 必须拥有自己的 `zoomMapping`，不能默认沿用。

当前方案的限制：

- fill/line 已被栅格化，无法直接拾取和编辑单个要素；
- 标签碰撞局限在单瓦片，不能保证跨瓦片一致；
- 不支持完整 Mapbox Style 规范、sprite、glyph、图标、复杂表达式和 line placement；
- Canvas 绘制和 MVT 解码在主线程，快速移动时会阻塞；
- 样式变化通常需要重绘整张纹理。

因此它的定位是“验证任意 MVT + Style 数据链路和制图层级”，不是最终矢量渲染器。

## 4. 目标总体架构

```text
Application / Viewer
├─ ProjectState
├─ ViewState
├─ Command / UndoRedo
│
├─ GIS Core
│  ├─ CrsRegistry / CoordinateTransform
│  ├─ LayerManager
│  ├─ DataSourceRegistry
│  ├─ TileScheduler
│  ├─ MemoryCache / PersistentCache
│  ├─ FeatureStore / SpatialIndex
│  ├─ SelectionSet
│  └─ EditSession
│
├─ View
│  ├─ GlobeView3D
│  │  ├─ GlobeCameraController
│  │  ├─ GlobeLodSelector
│  │  ├─ TerrainSurface
│  │  ├─ DrapedRasterPass
│  │  ├─ VectorPass
│  │  ├─ SymbolPass
│  │  └─ PickingPass
│  └─ MapView2D（后续）
│     ├─ MapCameraController
│     ├─ PlanarTileRenderer
│     ├─ VectorPass2D
│     └─ PickingPass2D
│
└─ DataSource Adapter
   ├─ XYZ / TMS / WMTS
   ├─ MVT / Mapbox Style
   ├─ MBTiles
   ├─ Terrain RGB / Heightmap / Quantized Mesh
   ├─ GeoJSON / WFS / 本地要素库
   └─ 未来 3D Tiles / I3S / 点云
```

核心原则是“Provider 获取与解码数据，Layer 表达用户语义和显示状态，Renderer 把图层变成当前视图的 GPU 资源”。Provider 不应直接拥有 UI，Renderer 不应自己决定服务 URL。

## 5. 下一阶段：图层与服务管理

### 5.1 LayerManager

需要从当前单 provider 切换升级为有序图层栈。建议公共图层状态至少包含：

```ts
type LayerState = {
  id: string;
  name: string;
  kind: 'imagery' | 'terrain' | 'vector-tile' | 'feature' | 'annotation';
  sourceId: string;
  visible: boolean;
  opacity: number;
  order: number;
  minCameraLevel?: number;
  maxCameraLevel?: number;
  blendMode?: 'normal' | 'multiply' | 'screen';
  attribution?: string;
};
```

必须支持：

- 多个影像/矢量服务同时叠加，而不是销毁式切换；
- 独立可见性、透明度、排序、层级范围和样式；
- 底图组互斥与业务覆盖层非互斥；
- 图层状态序列化到工程文件；
- provider 切换时复用相同 source 的缓存；
- 每层独立错误状态和署名聚合。

地形层是表面定义，通常同一视图只有一个主地形，但可以拥有多个地形源优先级和局部覆盖。影像和贴地矢量则组合到该表面上。

### 5.2 DataSource 与层级映射

建议每个数据源显式声明：

```ts
type TileSourceMetadata = {
  id: string;
  scheme: 'xyz' | 'tms';
  crs: string;
  tileSize: number;
  minDataLevel: number;
  maxDataLevel: number;
  bounds?: [number, number, number, number];
  format: 'png' | 'jpeg' | 'webp' | 'pbf' | 'terrain-rgb' | 'heightmap';
  zoomMapping: (cameraLevel: number, geometryLevel: number) => number;
};
```

层级偏移只是最简单映射。后续应支持分段校准，例如全球尺度减 2、城市尺度减 1；并允许样式 zoom 与请求 zoom 分开。

### 5.3 TileScheduler 与缓存

请求调度必须从 RasterTileLayer 抽出为共享服务：

- 优先级由可见性、屏幕误差、中心距离、图层类型和父级回退需求共同决定；
- 相机移动后取消不再需要的 fetch/worker 任务；
- 限制全局并发和每域名并发；
- 同一 `sourceId/z/x/y/variant` 请求去重；
- 解码结果缓存与 GPU 资源缓存分开；
- 缓存键包含 source、数据层级、坐标、样式版本、像素比和格式；
- 内存使用字节预算，不只按瓦片数量；
- 可选 IndexedDB/OPFS/桌面文件持久缓存；
- 明确服务授权、过期时间和禁止持久缓存的策略。

## 6. 下一阶段：地形渲染

### 6.1 地形数据模型

地形不能只在 shader 中临时读取高度。CPU/Worker 必须持有可查询高度：

```ts
type TerrainTileData = {
  id: TileId;
  width: number;
  height: number;
  heights: Float32Array;
  minimumHeight: number;
  maximumHeight: number;
  geometricError: number;
  boundingVolume: unknown;
  childMask?: number;
};
```

Provider 负责把 Terrain RGB、16 位 Heightmap、Quantized Mesh 或本地 DEM 解码成统一数据。Renderer 可以选择规则网格或量化网格，但 `sampleHeight(lon, lat)`、拾取、贴地和编辑必须读取同一权威高度。

当前阶段已落地 `TerrainRgbProvider + TerrainTileLayer`：支持 TileJSON/URL 模板、XYZ/TMS、Mapbox/Terrarium 编码、CPU Float32 高度场、GPU 高度纹理、祖先回退、基础高度查询，以及影像/矢量纹理和经纬网共享位移。它是只读地形第一版；下面的 terrain SSE、skirt、geomorph、边界法线与编辑增量仍按计划继续实现。

### 6.2 地形 LOD

地形可以共享球面四叉树和可见性遍历，但拆分误差不能只用瓦片经纬跨度。应使用：

```text
terrainSSE = geometricError × focalPixels / cameraDistance
```

影像清晰度和地形几何误差可以产生不同的期望等级。最终选择器应支持一个空间节点、多个 consumer 的需求汇总，而不是为每层建立互相冲突的四叉树。

父子替换规则：

1. 父地形保持显示，直到可见子地形全部解码并上传 GPU。
2. 父影像可以覆盖子地形，子影像未就绪时继续用父纹理 UV 回退。
3. 子节点失败时保留父节点，不允许裸露椭球。
4. 数据源局部无覆盖时逐级回退或显示无数据策略。

### 6.3 地形接缝与法线

至少实现：

- 同层相邻瓦片共享边界高度；
- 不同层级边界约束到粗层采样；
- skirt 遮挡残余裂缝；
- geomorph 或高度混合减轻父子跳变；
- 基于真实高度的法线，而不是未位移椭球法线；
- 邻接瓦片到达后更新边界法线；
- 高度范围进入包围体，避免山峰被地平线/视锥误裁。

Skirt 是保险，不是用来掩盖错误坐标或错误 LOD 覆盖的主要方案。

### 6.4 地形渲染通道

建议顺序：

1. TerrainDepth/Surface：绘制真实地形深度和基础材质。
2. DrapedRaster：卫星、栅格业务层按顺序混合到地形表面。
3. DrapedVector：需要贴地的线面，可采用 GPU 向量或中间纹理。
4. 3D Vector/Model：有高度、挤出或绝对高程的对象。
5. Symbol：统一屏幕空间标注和图标。
6. Selection/Edit Overlay：选择高亮、控制点、捕捉反馈。

初期可在一个 terrain shader 中组合有限数量纹理；长期应使用纹理数组、virtual texture、预合成或多 pass，不能无限增加 sampler。

## 7. 下一阶段：地形编辑

地形编辑采用“不可变基础地形 + 可撤销增量层”，不要直接改写原始服务瓦片。

### 7.1 编辑数据

```text
BaseTerrain（只读）
  + TerrainDeltaLayer A（工程修改）
  + 临时 PreviewDelta（交互中的笔刷）
  = EffectiveTerrain
```

增量可表示为稀疏高度 patch、规则栅格块或笔刷操作日志。每次操作保存影响范围、旧值/逆操作和新值，进入统一 Undo/Redo Command。

### 7.2 编辑流程

1. 屏幕射线与当前真实地形求交。
2. 在局部 ENU 坐标中执行抬高、降低、平整、平滑、坡面或填挖方笔刷。
3. 更新受影响最高分辨率 patch。
4. 同步修改相邻瓦片共享边界。
5. 自底向上重建父级 min/max、误差和降采样高度。
6. 标记地形网格、法线、包围体、拾取索引和阴影为 dirty。
7. 增量保存到工程文件/数据库，不覆盖基础 DEM。

### 7.3 精度与事务

- 编辑计算使用局部 ENU double；GPU 只显示结果。
- 大范围编辑在 Worker 中分块，主线程只负责预览。
- 事务提交前验证无 NaN、边界连续、合理高程范围和版本冲突。
- 保存格式必须记录 CRS、垂直基准、单位、基础数据源版本和 patch 层级。
- 后期协作编辑需要要素/patch 版本，而不是最后写入者直接覆盖。

## 8. 原生矢量瓦片渲染路线

### 8.1 通用性边界

替换 `public/En.json` 为其他样式并不保证完整显示。能否通用取决于：

- Style 是否为 Mapbox Style v8；
- source 是不是 MVT，tiles URL 是否可访问且允许 CORS；
- `source-layer` 名称是否与 MVT 内容一致；
- 是否依赖 sprite、glyph、icon、line-placement、heatmap、hillshade、fill-extrusion；
- 使用的 filter/expression 是否在引擎支持范围；
- XYZ/TMS、bounds、minzoom/maxzoom 和 zoomMapping 是否正确。

短期应加入样式能力扫描器：加载样式后列出支持、降级和不支持的 layer/expression，而不是静默画错。

### 8.2 最终矢量管线

```text
MVT bytes
  → Worker 解码
  → FeatureTable + Geometry
  → Style evaluation
  → RenderBucket(fill/line/circle/extrusion/symbol)
  → GPU buffers
  → Globe/Map View render pass
```

需要分离：

- `VectorTileSource`：请求和元数据；
- `MvtDecoder`：PBF → 要素；
- `StyleCompiler`：样式 JSON → 可执行表达式和 layer program；
- `VectorBucketBuilder`：三角化面、构造线 join/cap、点实例；
- `VectorRenderer`：GPU buffer、材质和 draw batching；
- `SymbolLayout`：跨瓦片屏幕碰撞、重复距离、淡入淡出和稳定锚点；
- `FeatureIndex`：拾取、属性查询和编辑定位。

### 8.3 跨瓦片标注

标注候选由可见 MVT 产生，但最终布局必须在整个 viewport 执行：

- 使用 canonical feature id/文本/图层去重；
- 国家、省、市、道路和水域分级排序；
- 处理瓦片 wrap 和父子层级重复；
- 屏幕空间碰撞；
- 相机小幅移动时保持上帧选择，减少闪烁；
- 文本 glyph atlas 与 icon sprite atlas 统一缓存；
- 文字朝向、地平线遮挡和球背剔除在 SymbolPass 处理。

## 9. 点线面编辑与渲染

### 9.1 数据与渲染分离

可编辑要素保存在 `FeatureStore`，包含稳定 ID、几何、属性、CRS 和版本。GPU buffer 只是缓存，不能成为唯一数据源。

```ts
type Feature = {
  id: string | number;
  geometry: Geometry;
  properties: Record<string, unknown>;
  crs: string;
  revision: number;
};
```

渲染按图层、样式和空间瓦片构建 bucket。编辑一个要素只重建受影响 bucket，不重建整个图层。

### 9.2 高程模式

每个图层/要素必须声明：

- `clamp-to-ground`：贴合地形；
- `relative-to-ground`：相对地形高度；
- `absolute`：绝对椭球高/指定垂直基准；
- `3d`：完整三维坐标。

地形异步更新后，贴地要素必须重新采样；绝对高程要素不应随地形改变。

### 9.3 编辑系统

需要统一支持：

- GPU picking ID pass + CPU 精确复核；
- 顶点、边、中点、交点和地形捕捉；
- 点移动、线增删节点、面环与洞编辑；
- 自相交、环方向、最小点数等几何验证；
- 选择集、高亮和编辑手柄独立 overlay；
- Command 模式 Undo/Redo；
- 编辑事务、dirty feature 和批量保存；
- 后续拓扑规则与共享边界联动。

所有编辑都在地理/局部 ENU 数据模型中完成，不直接拖动大地坐标下的 Three.js mesh 顶点。

## 10. MBTiles 接入设计

MBTiles 是 SQLite 容器，不是单纯 URL 模板。接入分三种运行环境：

### 10.1 桌面/服务端

通过原生 SQLite、Node 主进程或本地服务读取：

```sql
SELECT tile_data
FROM tiles
WHERE zoom_level = ? AND tile_column = ? AND tile_row = ?;
```

MBTiles 常用 TMS 行号，转换为：

```text
tmsY = (2^z - 1) - xyzY
```

### 10.2 浏览器

可选：

- 用户选择文件后使用 SQLite WASM/OPFS；
- 由本地 HTTP 服务暴露统一 TileProvider API；
- 大文件优先考虑 PMTiles/服务端切片，避免把整个 SQLite 读入内存。

浏览器方案必须支持随机读取、Worker 查询和文件生命周期，不能在每个瓦片请求时重新打开数据库。

### 10.3 统一适配器

`MbtilesSource` 初始化时读取 metadata：name、format、bounds、center、minzoom、maxzoom、scheme、compression。随后：

- PNG/JPEG/WebP 进入 Raster decoder；
- PBF（可能 gzip）进入 MVT decoder；
- TMS/XYZ 在 source adapter 中统一转换；
- 数据缺失返回明确的 `no-data`，触发祖先回退；
- sourceId 包含文件身份和修改版本，防止缓存串包。

MBTiles 只解决存储，不自动解决样式。矢量 MBTiles 仍需要匹配的 Style、sprite、glyph 和 source-layer 配置。

## 11. 相机、拾取与深度

当前已用 `GlobeCameraController` 替换 OrbitControls：左键旋转整个地球观察框架，右键围绕当前地表焦点旋转视角，中键围绕地表焦点改变仰角，滚轮沿视线按真实离地高度缩放，并提供 `flyTo` 与 ViewState。右键和中键始终保留有效地表焦点并限制倾角，避免相机转向太空后无法恢复。后续继续完善：

- 拖拽基于屏幕射线与椭球/地形交点；
- 缩放保持鼠标指向的地表位置；
- 相机与地形碰撞，支持最小离地高度；
- 连续 ViewState：中心经纬高、距离/尺度、heading、pitch、roll；
- 2D/3D 切换只转换 ViewState，不迁移全部图层 mesh；
- near/far 根据当前高度和可见范围更新；
- logarithmic depth 保留，同时评估 reversed depth/WebGPU 路线。

拾取分层：先通过深度/ID buffer 找到候选图层和对象，再在 CPU 高精度数据上求交。地形拾取不能只与参考椭球求交。

## 12. Worker 与 GPU 职责

### CPU/Worker 权威职责

- 网络/文件读取和解压；
- MVT、DEM、MBTiles 数据解码；
- 样式表达式求值与 bucket 构建；
- 地形几何误差、法线、包围体和高度查询结构；
- 要素空间索引、编辑、拓扑和事务；
- double 精度坐标转换。

### GPU 职责

- 高低位相对相机投影；
- 地形、影像、线面、实例点和文字 atlas 绘制；
- 颜色混合、光照、深度和选择 ID；
- 可选的高度纹理采样和视觉 morph，但不能成为唯一高度数据源。

不能为了“使用 GPU”把拾取、编辑所需的唯一数据藏进 shader。GIS 正确性以 CPU 权威数据为准，GPU 用于批量并行显示。

## 13. 分阶段实施计划

### 阶段 A：稳定当前内核

- 为坐标往返、XYZ 矩形、父子边界和高层级局部坐标增加自动测试。
- 为 LOD 增加不同 FOV、DPR、宽高比、极区和斜视覆盖测试。
- 抽出 TileScheduler、请求取消和按字节缓存预算。
- 建立浏览器 shader/截图回归测试。

验收：卫星/矢量在 LOD2–27、不同窗口和特殊角度无非极区孔洞；慢网始终有祖先覆盖。

### 阶段 B：LayerManager 与多服务

- 实现 DataSourceRegistry、LayerManager 和有序图层栈。
- 支持多影像叠加、底图组切换、透明度、顺序、署名和工程序列化。
- source 级 zoomMapping 取代演示硬编码。

验收：至少两个影像服务和一个矢量服务可独立开关/排序，切换不清空无关缓存。

### 阶段 C：只读地形

- 先接入一种明确格式（建议 Terrain RGB/规则 Heightmap），Worker 解码到统一 TerrainTileData。
- 实现 terrain SSE、父子回退、skirt、边界连续、真实法线和高度采样。
- 影像贴合地形；相机碰撞和真实地形拾取。

验收：快速飞行和慢网无裂缝/空洞；相邻层级接缝稳定；CPU 高度查询与画面一致。

### 阶段 D：地形编辑

- 实现 TerrainDeltaLayer、笔刷预览、Undo/Redo 和 dirty tile 传播。
- 边界同步、父级重建、法线和包围体增量更新。
- 工程保存/加载和基础数据版本校验。

验收：跨瓦片笔刷无接缝，撤销重做完全可逆，重启工程后结果一致。

### 阶段 E：原生矢量与点线面编辑

- Worker MVT 解码、StyleCompiler、GPU bucket 和 FeatureIndex。
- 独立 SymbolPass 完成跨瓦片碰撞。
- FeatureStore、选择、捕捉、编辑命令和高程模式。

验收：要素可拾取查询；标注跨瓦片稳定；局部编辑只更新相关 bucket。

### 阶段 F：MBTiles 与离线工程

- 实现栅格/矢量 MbtilesSource、TMS 转换、metadata 和压缩处理。
- 浏览器文件/WASM 或桌面本地服务适配。
- 持久缓存、离线包管理和数据授权策略。

验收：同一图层可以在在线 XYZ/MVT 与本地 MBTiles 之间切换，地理位置、层级和样式一致。

## 14. 测试矩阵与完成定义

每个阶段都必须覆盖：

- 空间：赤道、日期变更线、中高纬、南北极附近；
- 相机：正视、斜视、地平线切边、快速旋转、连续缩放；
- 层级：最小级、服务最大级前后、20–27 高精度级；
- viewport：窄屏、宽屏、高 DPR、动态 resize；
- 网络：正常、延迟、乱序、404、取消、断网恢复；
- LOD：同层邻接、父子邻接、瓦片预算耗尽；
- 数据：XYZ/TMS、不同 bounds、缺瓦片、不同 zoomMapping；
- 精度：坐标往返、厘米/米级局部移动、高层级线面稳定；
- 资源：缓存上限、图层反复开关、provider 替换、dispose 后无泄漏。

“完成”不能只定义为画面看起来正确，还必须同时满足：覆盖不变量、控制台无错误、请求数量合理、内存受控、CPU 查询与 GPU 显示一致、自动测试和真实浏览器测试通过。

## 15. 当前最优先的下一步

下一步不应直接把地形代码塞进 `RasterTileLayer`。正确顺序是：

1. 抽出共享 `TileScheduler` 与资源状态机。
2. 建立 `LayerManager + DataSourceRegistry`，让卫星、矢量和未来地形都成为可管理图层。
3. 定义 `TerrainProvider`、`TerrainTileData` 和高度查询 API。
4. 接入只读地形，先解决 LOD、接缝、拾取和影像贴合。
5. 在稳定的只读地形数据模型上增加非破坏式 TerrainDeltaLayer。

这条顺序能避免地形、影像、矢量各自复制一套请求、缓存和四叉树，也为后续 MBTiles 与点线面编辑保留统一入口。
